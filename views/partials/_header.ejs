<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="google-site-verification" content="ebGS65zSHnfbgVLUfh3m4yU7C1f7ViCXkZbwS5KvaTY" />
    <link rel="shortcut icon" href="/img/favicon/infinitidev.png" type="image/x-icon">
    <meta name="google-adsense-account" content="ca-pub-9292583891027825">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    
</head>

<body>
    <% if (viewName !== 'notfound') { %>
    <!-- Offcanvas Navbar (Visible on Small Screens) -->
    <nav class="navbar bg-primary position-sticky top-0 z-1 d-flex d-md-none">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src="/img/codewithinfinitidev.png" alt="logo" width="110px">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas"
                data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="offcanvas offcanvas-start" tabindex="-1" id="offcanvasNavbar"
                aria-labelledby="offcanvasNavbarLabel">
                <div class="offcanvas-header bg-primary">
                    <h5 class="offcanvas-title" id="offcanvasNavbarLabel">
                        <a href="/">
                            <img src="/img/codewithinfinitidev.png" alt="logo" width="110px">
                        </a>
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                </div>
                <div class="offcanvas-body">
                        <% if (!['/auth/login', '/auth/register', '/forgot-password', '/reset-password'].includes(url) && viewName !== 'index' && viewName !== 'terms' && viewName !== 'privacy') { %>
                            <%- include('_sidebar') %>
                        <% } %>
                </div>
            </div>
        </div>
    </nav>
    <% } %>
<% if (!['/auth/login', '/auth/register', '/forgot-password', '/reset-password'].includes(url) && viewName !== 'index' && viewName !== 'terms' && viewName !== 'privacy' && viewName !== 'notfound') { %>
    <div class="row p-0 m-0 main-content-wrapper bg-light">
        <div class="col-lg-2 bg-secondary col-md-3 d-lg-inline d-md-inline d-none h-100 border-end border-primary">
            <%- include('_sidebar') %>
        </div>
        <div class="col-lg-10 col-md-9 col-sm-12 py-3 overflow-y-auto vh-100">
            <div class="mx-lg-5 mx-md-3 mx-sm-1">
<% } %>
