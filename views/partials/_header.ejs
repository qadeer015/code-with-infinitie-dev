<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="google-site-verification" content="ebGS65zSHnfbgVLUfh3m4yU7C1f7ViCXkZbwS5KvaTY" />
    <link rel="shortcut icon" href="/img/favicon/infinitidev.png" type="image/x-icon">
    <meta name="google-adsense-account" content="ca-pub-9292583891027825">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

</head>

<body>
    <!-- Desktop Header -->
    <nav class="navbar bg-primary fixed-top d-none d-md-flex">
        <div class="container-fluid">
            <div class="d-flex align-items-center gap-2">
                <a class="navbar-brand" href="/">
                    <img src="/img/codewithinfinitidev.png" alt="logo" width="110px">
                </a>
            </div>
            <div class="d-flex align-items-center gap-5">
                <form class="d-flex rounded-pill overflow-hidden" action="/search">
                    <input class="form-control m-0 rounded-start-pill" type="search" name="query" placeholder="Search" aria-label="Search" required>
                    <button class="btn m-0 btn-success rounded-end-pill fw-bold" type="submit"><i class="bi bi-search"></i></button>
                </form>
                <div class="d-flex align-items-center">
                    <% if (user) { %>
                    <div class="dropstart">
                        <a class="" href="#" role="button"
                            data-bs-toggle="dropdown" aria-expanded="false">
                          <img src="<%= user.avatar %>" alt="user avatar" width="35px" class="rounded-circle">
                        </a>
                        <ul class="dropdown-menu">
                            <li><p class="dropdown-item lead"><%= user.name %></p></li>
                            <li><p class="dropdown-item m-0">(<%= user.role %>)</p></li>
                            <li><a class="dropdown-item" href="/users/<%= user.id %>/profile">Profile</a></li>
                            <li><a class="dropdown-item" href="/auth/logout" onclick="return confirm('Are you sure you want to logout?')" >Logout</a></li>
                        </ul>
                    </div>
                    <% } %>  
                </div>
            </div>
        </div>
    </nav>
    
    <!-- Offcanvas Navbar (Visible on Small Screens) -->
    <nav class="navbar bg-primary fixed-top d-flex d-md-none">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src="/img/codewithinfinitidev.png" alt="logo" width="110px">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas"
                data-bs-target="#offcanvasNavbar" aria-controls="offcanvasNavbar" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNavbar"
                aria-labelledby="offcanvasNavbarLabel">
                <div class="offcanvas-header bg-primary">
                    <h5 class="offcanvas-title" id="offcanvasNavbarLabel">
                        <a href="/">
                            <img src="/img/codewithinfinitidev.png" alt="logo" width="110px">
                        </a>
                    </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                </div>
                <div class="offcanvas-body">
                    <form class="d-flex rounded-pill overflow-hidden" action="/search">
                        <input class="form-control m-0 rounded-start-pill" type="search" name="query" placeholder="Search" aria-label="Search" required>
                        <button class="btn m-0 btn-success rounded-end-pill fw-bold" type="submit"><i class="bi bi-search"></i></button>
                    </form>
                    <div class="my-3">
                        <% if (user) { %>
                            <div class="dropdown">
                                <a class="" href="#" role="button"
                                    data-bs-toggle="dropdown" aria-expanded="false">
                                  <img src="<%= user.avatar %>" alt="user avatar" width="35px" class="rounded-circle">
                                </a>
                                <ul class="dropdown-menu">
                                    <li><p class="dropdown-item lead"><%= user.name %></p></li>
                                    <li><p class="dropdown-item m-0">(<%= user.role %>)</p></li>
                                    <li><a class="dropdown-item" href="/users/profile/<%= user.id %>">Profile</a></li>
                                    <li><a class="dropdown-item" href="/auth/logout" onclick="return confirm('Are you sure you want to logout?')" >Logout</a></li>
                                </ul>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </nav>