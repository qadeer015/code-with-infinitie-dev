<%- include('../partials/_header', { title: 'Sessions' }) %>
    <div class="d-flex align-items-center justify-content-between">
        <h4 class="fw-bold pt-4 pb-1">Lectures</h4>
        <a href="/users/admin/sessions/new" class="btn btn-primary">New Session</a>
    </div>
    <div class="row pb-5">
        <div class="col-12 table-responsive min-vh-100">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Name</th>
                        <th scope="col">Description</th>
                        <th scope="col">Start Date</th>
                        <th scope="col">End Date</th>
                        <th scope="col">Status</th>
                        <th scope="col">CreatedBy</th>
                        <th scope="col">Actions</th>
                    </tr>
                </thead>
                <tbody id="sessions-data" class="table-group-divider">
                    <!-- lecture details will go here -->
                </tbody>
            </table>
        </div>
    </div>
    <script>

        const sessionsData = document.getElementById('sessions-data');

        document.addEventListener("DOMContentLoaded", function () {
            fetchCourses();
        })

        function fetchCourses() {
            const response = fetch("/users/admin/sessions/get-all", {
                method: "GET",
                headers: {
                    "Content-Type": "application/json",
                },
                cache: "no-cache",
            }).then(response => {
                if (!response.ok) {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }

                return response.json();
            }).then(data => {
                sessionsData.innerHTML = '';
                console.log(data);
                data.forEach(lecture => {
                    sessionsData.innerHTML += renderSessions(lecture);
                });
            }).catch(error => {
                console.error("Error fetching assignments:", error);
                sessionsData.innerHTML = `
                    <div class="error-message">
                        <p>Failed to load assignments. Please try again later.</p>
                    </div>
                `;
            });
        }

        function renderSessions(session) {
            return `
                <tr>
                    <td>${session.name}</td>
                    <td>${session.description}</td>
                    <td>${session.start_date}</td>
                    <td>${session.end_date}</td>
                    <td>${session.status}</td>
                    <td>${session.created_by_name}</td>
                    <td>
                        <div class="dropdown">
                            <button class="bg-transparent rounded-circle border-1 border-muted px-1 outline-0" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <i class="bi bi-three-dots-vertical"></i>
                            </button>
                            <ul class="dropdown-menu">
                                <a class="text-decoration-none text-dark" href="/users/admin/sessions/${session.id}/edit">
                                <li class="dropdown-item mb-1">
                                    <i class="bi bi-pencil"></i>
                                    Edit
                                </li>
                                </a>
                                <li class="dropdown-item mb-1">
                                    <form action="/users/admin/sessions/${session.id}/delete" method="post" onsubmit="return confirm('Are you sure you want to delete this lecture?')">
                                        <i class="bi bi-trash"></i>
                                        <input type="submit" class="w-100 text-start bg-transparent border-0 outline-0" value="Delete">
                                    </form>
                                </li>
                            </ul>
                        </div>
                    </td>
                </tr>
            `;
        }
    </script>
    <%- include('../partials/_footer') %>